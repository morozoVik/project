[project]
name = "project"
version = "0.1.0"
description = "1"
authors = [
    {name = "Ilya Morozov", email = "ulb9@mail.ru"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests"
]

[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"